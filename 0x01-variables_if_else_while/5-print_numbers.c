#include <stdio.h>
/**
 * main - entry point of the code
 * Return: 0
 */
int main(void)
{
	int decimal;

	for (decimal = 0; decimal <= 9; decimal++)
	{
		printf("%d", decimal);
	}
	putchar('\n');
	return (0);
}

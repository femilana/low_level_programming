#include "main.h"

/**
 * mul - multiplies the int 'a' and 'b'
 * @a: The first int to be multiplied
 * @b: The secound int to be multiplied
 * Return: 0
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);

	return (result);
}

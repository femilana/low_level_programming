#include <stdio.h>

/**
 * main - entry point
 * @argv: argument array
 * @argc: argument counter
 * Return: argc
 */

int main(int argc, char *argv[])
{
	printf("the program name is %s\n", argv[0]);
	return (argc);

}
